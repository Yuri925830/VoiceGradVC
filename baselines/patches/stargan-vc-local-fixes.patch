diff --git a/convert.py b/convert.py
index 3fefa46..ac1e115 100644
--- a/convert.py
+++ b/convert.py
@@ -39,7 +39,7 @@ def audio_transform(wav_filepath, scaler, kwargs, device):
         audio, _ = librosa.effects.trim(audio, top_db=top_db, frame_length=2048, hop_length=512)
     if fs != fs_:
         #print('resampling.')
-        audio = librosa.resample(audio, fs_, fs)
+        audio = librosa.resample(audio, orig_sr=fs_, target_sr=fs)
     melspec_raw = logmelfilterbank(audio,fs, fft_size=flen,hop_size=fshift,
                                     fmin=fmin, fmax=fmax, num_mels=num_mels)
     melspec_raw = melspec_raw.astype(np.float32) # n_frame x n_mels
@@ -205,4 +205,4 @@ def main():
 
 
 if __name__ == '__main__':
-    main()
\ No newline at end of file
+    main()
diff --git a/extract_features.py b/extract_features.py
index 1f9acf3..0ab2160 100644
--- a/extract_features.py
+++ b/extract_features.py
@@ -52,7 +52,13 @@ def logmelfilterbank(audio,
     # get mel basis
     fmin = 0 if fmin is None else fmin
     fmax = sampling_rate / 2 if fmax is None else fmax
-    mel_basis = librosa.filters.mel(sampling_rate, fft_size, num_mels, fmin, fmax)
+    mel_basis = librosa.filters.mel(
+        sr=sampling_rate,
+        n_fft=fft_size,
+        n_mels=num_mels,
+        fmin=fmin,
+        fmax=fmax,
+    )
 
     return np.log10(np.maximum(eps, np.dot(spc, mel_basis.T)))
 
@@ -75,7 +81,7 @@ def extract_melspec(src_filepath, dst_filepath, kwargs):
             audio, _ = librosa.effects.trim(audio, top_db=top_db, frame_length=2048, hop_length=512)
         if fs != fs_:
             #print('resampling.')
-            audio = librosa.resample(audio, fs_, fs)
+            audio = librosa.resample(audio, orig_sr=fs_, target_sr=fs)
         melspec_raw = logmelfilterbank(audio,fs, fft_size=flen,hop_size=fshift,
                                         fmin=fmin, fmax=fmax, num_mels=num_mels)
         melspec_raw = melspec_raw.astype(np.float32)
@@ -156,4 +162,4 @@ def main():
     )
 
 if __name__ == '__main__':
-    main()
\ No newline at end of file
+    main()
diff --git a/recipes/run_test_arctic_4spk.sh b/recipes/run_test_arctic_4spk.sh
index 328544e..418b89c 100755
--- a/recipes/run_test_arctic_4spk.sh
+++ b/recipes/run_test_arctic_4spk.sh
@@ -3,9 +3,10 @@
 # Copyright 2021 Hirokazu Kameoka
 # 
 # Usage:
-# ./run_test_arctic_4spk.sh [-g gpu] [-e exp_name] [-c checkpoint] [-v vocoder_type]
+# ./run_test_arctic_4spk.sh [-g gpu] [-d db_dir] [-e exp_name] [-c checkpoint] [-v vocoder_type]
 # Options:
 #     -g: GPU device#  
+#     -d: Path to the test dataset directory
 #     -e: Experiment name (e.g., "conv_exp1")
 #     -c: Model checkpoint to load (0 indicates the newest model)
 #     -v: Vocoder type ("hifigan.v1" or "parallel_wavegan.v1")
@@ -16,9 +17,13 @@ gpu=0
 checkpoint=0
 vocoder_type="hifigan.v1"
 
-while getopts "g:e:c:v:" opt; do
+export NUMBA_CACHE_DIR="${NUMBA_CACHE_DIR:-/tmp/voicegrad_numba_cache}"
+export MPLCONFIGDIR="${MPLCONFIGDIR:-/tmp/voicegrad_matplotlib_cache}"
+
+while getopts "g:d:e:c:v:" opt; do
        case $opt in
               g ) gpu=$OPTARG;;
+              d ) db_dir=$OPTARG;;
               e ) exp_name=$OPTARG;;
 			  c ) checkpoint=$OPTARG;;
 			  v ) vocoder_type=$OPTARG;;
@@ -48,4 +53,4 @@ python convert.py -g ${gpu} \
 	--experiment_name ${exp_name} \
 	--vocoder ${vocoder_type} \
 	--voc_dir ${vocoder_dir} \
-	--checkpoint ${checkpoint}
\ No newline at end of file
+	--checkpoint ${checkpoint}
diff --git a/recipes/run_train_arctic_4spk.sh b/recipes/run_train_arctic_4spk.sh
index b769e87..15f68e6 100755
--- a/recipes/run_train_arctic_4spk.sh
+++ b/recipes/run_train_arctic_4spk.sh
@@ -3,7 +3,7 @@
 # Copyright 2021 Hirokazu Kameoka
 # 
 # Usage:
-# ./run_train_arctic_4spk.sh [-g gpu] [-d db_dir] [-a arch_type] [-l loss_type] [-s stage] [-e exp_name]
+# ./run_train_arctic_4spk.sh [-g gpu] [-d db_dir] [-a arch_type] [-l loss_type] [-s stage] [-e exp_name] [-n epochs] [-p snapshot] [-b batch_size] [-r resume]
 # Options:
 #     -g: GPU device# 
 #     -d: Path to the dataset directory
@@ -11,6 +11,10 @@
 #     -l: Loss type ("cgan", "wgan", or "lsgan")
 #     -s: Stage to start (0 or 1)
 #     -e: Experiment name (e.g., "exp1")
+#     -n: Number of epochs
+#     -p: Snapshot interval in epochs
+#     -b: Batch size
+#     -r: Resume checkpoint epoch
 
 # Default values
 db_dir="/misc/raid58/kameoka.hirokazu/db/arctic/wav/training"
@@ -20,8 +24,15 @@ arch_type="conv"
 loss_type="wgan"
 start_stage=0
 exp_name="conv_wgan_exp1"
+epochs=2000
+snapshot=200
+batch_size=12
+resume=0
 
-while getopts "g:d:a:l:s:e:" opt; do
+export NUMBA_CACHE_DIR="${NUMBA_CACHE_DIR:-/tmp/voicegrad_numba_cache}"
+export MPLCONFIGDIR="${MPLCONFIGDIR:-/tmp/voicegrad_matplotlib_cache}"
+
+while getopts "g:d:a:l:s:e:n:p:b:r:" opt; do
        case $opt in
               g ) gpu=$OPTARG;;
               d ) db_dir=$OPTARG;;
@@ -29,6 +40,10 @@ while getopts "g:d:a:l:s:e:" opt; do
               l ) loss_type=$OPTARG;;
               s ) start_stage=$OPTARG;;
               e ) exp_name=$OPTARG;;
+              n ) epochs=$OPTARG;;
+              p ) snapshot=$OPTARG;;
+              b ) batch_size=$OPTARG;;
+              r ) resume=$OPTARG;;
        esac
 done
 
@@ -55,5 +70,9 @@ if [[ ${start_stage} -le 1 ]]; then
               --arch_type ${arch_type} \
               --loss_type ${loss_type} \
               --experiment_name ${exp_name} \
+              --epochs ${epochs} \
+              --snapshot ${snapshot} \
+              --batch_size ${batch_size} \
+              --resume ${resume} \
               ${cond}
-fi
\ No newline at end of file
+fi
