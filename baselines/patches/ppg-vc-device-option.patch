diff --git a/convert_from_wav.py b/convert_from_wav.py
index b4855b1..3a1cb5a 100644
--- a/convert_from_wav.py
+++ b/convert_from_wav.py
@@ -94,7 +94,10 @@ def build_ppg2mel_model(model_config, model_file, device):
 
 @torch.no_grad()
 def convert(args):
-    device = 'cuda'
+    if args.device == "auto":
+        device = "cuda" if torch.cuda.is_available() else "cpu"
+    else:
+        device = args.device
     ppg2mel_config = HpsYaml(args.ppg2mel_model_train_config) 
     output_dir = args.output_dir
     os.makedirs(output_dir, exist_ok=True)
@@ -202,6 +205,13 @@ def get_parser():
         default="vc_gens_vctk_oneshot",
         help="Output folder to save the converted wave."
     )
+    parser.add_argument(
+        "--device",
+        type=str,
+        default="auto",
+        choices=["auto", "cuda", "cpu"],
+        help="Torch device to run conversion on.",
+    )
     
     return parser
 
